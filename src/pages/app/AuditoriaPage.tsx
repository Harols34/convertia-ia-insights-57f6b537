import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { ScrollText } from "lucide-react";
export default function AuditoriaPage() {
  return <PlaceholderModule title="Auditoría y Logs" description="Registro detallado de eventos, acciones y cambios en la plataforma." icon={ScrollText} />;
}
