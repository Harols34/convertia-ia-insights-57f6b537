import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { Users } from "lucide-react";
export default function UsuariosPage() {
  return <PlaceholderModule title="Usuarios" description="Gestiona usuarios, invitaciones y accesos del tenant." icon={Users} />;
}
