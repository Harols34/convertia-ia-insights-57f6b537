import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { Brain } from "lucide-react";
export default function DashboardsIAPage() {
  return <PlaceholderModule title="Dashboards con IA" description="Genera tableros inteligentes con lenguaje natural." icon={Brain} />;
}
