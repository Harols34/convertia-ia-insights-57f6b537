import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { HelpCircle } from "lucide-react";
export default function SoportePage() {
  return <PlaceholderModule title="Soporte" description="Centro de ayuda, documentación y contacto con soporte técnico." icon={HelpCircle} />;
}
