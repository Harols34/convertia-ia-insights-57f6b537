import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { Download } from "lucide-react";
export default function ExportacionesPage() {
  return <PlaceholderModule title="Exportaciones" description="Exporta dashboards y reportes a PDF, PPTX, Excel y CSV." icon={Download} />;
}
