import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { Settings } from "lucide-react";
export default function ConfiguracionPage() {
  return <PlaceholderModule title="Configuración" description="Configuración del tenant, branding, módulos y preferencias." icon={Settings} />;
}
