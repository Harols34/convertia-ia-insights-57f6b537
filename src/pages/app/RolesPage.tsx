import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { ShieldCheck } from "lucide-react";
export default function RolesPage() {
  return <PlaceholderModule title="Roles y Permisos" description="Configura roles, permisos granulares y control de acceso." icon={ShieldCheck} />;
}
