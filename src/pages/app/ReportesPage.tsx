import { PlaceholderModule } from "@/components/app/PlaceholderModule";
import { FileBarChart } from "lucide-react";
export default function ReportesPage() {
  return <PlaceholderModule title="Reportes" description="Genera y gestiona reportes ejecutivos profesionales." icon={FileBarChart} />;
}
